let arrayDeStrings = ['normal', 'dicotonia', 'tararea'];
let arrayEnMayusculas = [];

arrayDeStrings.forEach(function(may) {
    arrayEnMayusculas.push(may.toUpperCase());
});

console.log(arrayEnMayusculas); 